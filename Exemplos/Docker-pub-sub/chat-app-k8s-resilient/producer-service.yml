apiVersion: v1
kind: Service
metadata:
  name: producer-service
  namespace: kafka-demo
spec:
  type: NodePort
  selector:
    app: producer-service
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
