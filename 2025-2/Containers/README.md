Comandos interessantes:

```bash
$ sudo docker ps –a
$ sudo docker stop prog_web
$ sudo docker rm prog_web
$ sudo docker rmi fa255d39a2d6
$ sudo docker image prune -a
$ sudo docker images
$ sudo docker exec -it prog_web /usr/bin/bash
$ sudo docker rmi static-site_web
$ sudo docker logs -f chat-app_consumer-service_1
$ sudo docker inspect chat-app_consumer-service_1
```

